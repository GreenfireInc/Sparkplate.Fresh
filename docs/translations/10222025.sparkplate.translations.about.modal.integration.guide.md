# About Modal Translation Integration Guide

**Date:** October 22, 2025  
**Project:** Sparkplate.Fresh  
**Category:** Translations - Integration Guide  
**Scope:** Step-by-Step Component Integration  

## Quick Start

This guide provides step-by-step instructions for integrating the About Modal Translation System into each component of the About modal.

## 🚀 Integration Steps

### 1. About.vue (Main Modal Container)

**File:** `src/components/global/About.vue`

#### Current Code (Lines 75-82):
```javascript
modes: {
  main: 'Main',
  notes: 'Release Notes / Changelog',
  greenfire: 'Greenfire',
  universe: 'Universe',
  contribute: 'Contribute',
  donations: 'Donations'
}
```

#### Updated Code:
```vue
<script>
import { useAboutTranslations } from '@/composables/useAboutTranslations'
// ... existing imports

export default {
  name: 'AboutView',
  // ... existing components
  setup() {
    const { 
      main, 
      releaseNotes, 
      greenfire, 
      universe, 
      contribute, 
      donations 
    } = useAboutTranslations()
    
    return {
      main,
      releaseNotes,
      greenfire,
      universe,
      contribute,
      donations
    }
  },
  data: () => ({
    activeMode: 'main',
    modes: {}, // Will be populated in created()
    isVisible: false,
    version
  }),
  created() {
    // Populate modes with translated values
    this.modes = {
      main: this.main,
      notes: this.releaseNotes,
      greenfire: this.greenfire,
      universe: this.universe,
      contribute: this.contribute,
      donations: this.donations
    }
    
    window.ipcRenderer.on('about-modal-open', this.toggleModal)
  }
  // ... rest of component
}
```

### 2. Main.vue (System Information Tab)

**File:** `src/components/modals/about/Main.vue`

#### Current Code (Lines 13-22):
```html
<li v-if="hostname"><b>Hostname:</b> {{ hostname }}</li>
<li v-if="os"><b>OS:</b> {{ os }}</li>
<li v-if="nodeVersion"><b>Node:</b> {{ nodeVersion }}</li>
<li v-if="electronVersion"><b>Electron:</b> v{{ electronVersion }}</li>
<li v-if="memorySize"><b>Installed RAM:</b> {{ memorySize }}</li>
<li v-if="processor"><b>CPU:</b> {{ processor }}</li>
```

#### Current Code (Lines 46-48):
```html
<button @click="clearStore" class="clear-store-btn">
  Clear Store
</button>
```

#### Updated Code:
```vue
<template>
  <div class="main-content-wrapper">
    <div class="container-fluid">
      <div class="row">
        <!-- Info section on the left -->
        <div class="col-6 info-column">
          <div>
            <br>
            <ul>
              <li v-if="hostnameValue"><b>{{ t('hostname') }}:</b> {{ hostnameValue }}</li>
              <NetworkStatus :vertical="true" :hideStatus="true" :showLocalIp="true" />
              <li v-if="osValue"><b>{{ t('os') }}:</b> {{ osValue }}</li>
              <li v-if="nodeVersionValue"><b>{{ t('node') }}:</b> {{ nodeVersionValue }}</li>
              <li v-if="electronVersionValue">
                <b>{{ t('electron') }}:</b> v{{ electronVersionValue }}
              </li>
              <li v-if="memorySizeValue"><b>{{ t('installedRAM') }}:</b> {{ memorySizeValue }}</li>
              <li v-if="processorValue"><b>{{ t('cpu') }}:</b> {{ processorValue }}</li>
              <GpuInfo />
              <LiveDateTime />
              <UptimeCounter />
            </ul>
          </div>
        </div>
        
        <!-- Logo section on the right -->
        <div class="col-6">
          <div class="center-content">
            <a href="https://www.greenfire.io" target="_blank">
              <img 
                src="/assets/icons/greenfire/sparkplate.png" 
                :alt="t('sparkplateLogo')" 
                style="width: 250px; height: auto;" 
              />
            </a>
          </div>
        </div>
      </div>
      
      <!-- Clear Store Button -->
      <div class="clear-store-container">
        <button @click="clearStore" class="clear-store-btn">
          {{ t('clearStore') }}
        </button>
      </div>
    </div>
  </div>
</template>

<script>
import { useAboutTranslations } from '@/composables/useAboutTranslations'
import NetworkStatus from '@/components/global/NetworkStatus.vue'
import UptimeCounter from '@/components/partials/time/UptimeCounter.vue'
import LiveDateTime from '@/components/partials/time/LiveDateTime.vue'
import GpuInfo from '@/components/partials/hardware/GpuInfo.vue'
import { version } from '../../../../package.json'

export default {
  name: 'AboutMain',
  components: { NetworkStatus, UptimeCounter, LiveDateTime, GpuInfo },
  setup() {
    const { t } = useAboutTranslations()
    return { t }
  },
  data: () => ({
    appVersion: version,
    electronVersionValue: '',
    hostnameValue: '',
    memorySizeValue: '',
    nodeVersionValue: '',
    osValue: '',
    processorValue: ''
  }),
  async created() {
    const appData = window.appData
    this.electronVersionValue = appData.electronVersion
    this.hostnameValue = appData.hostname
    this.memorySizeValue = appData.systemMemory
      ? this.formatBytes(appData.systemMemory)
      : ''
    this.nodeVersionValue = appData.nodeVersion
    this.osValue = appData.osVersion
    this.processorValue = appData.processor
  },
  methods: {
    formatBytes(bytes, decimals = 0) {
      if (!+bytes) return '0 Bytes'

      const kb = 1024
      const dm = decimals < 0 ? 0 : decimals
      const sizes = ['Bytes', 'KB', 'MB', 'GB', 'TB']

      const i = Math.floor(Math.log(bytes) / Math.log(kb))

      return `${parseFloat((bytes / Math.pow(kb, i)).toFixed(dm))} ${sizes[i]}`
    },
    clearStore() {
      localStorage.clear()
      sessionStorage.clear()
      console.log('Store cleared successfully')
    }
  }
}
</script>

<!-- ... existing styles -->
```

### 3. Notes.vue (Documentation Tab)

**File:** `src/components/modals/about/Notes.vue`

#### Current Code (Lines 6, 32, 38-39):
```html
<h3 class="sidebar-title">Documentation</h3>
<p>Loading documentation...</p>
<h3>Select a document to view</h3>
<p>Choose a file from the sidebar to view its contents.</p>
```

#### Updated Code:
```vue
<template>
  <div class="releaseNotes">
    <div class="docs-container">
      <!-- File Navigation -->
      <div class="docs-sidebar">
        <h3 class="sidebar-title">{{ t('documentation') }}</h3>
        <div class="file-list">
          <button 
            v-for="file in docFiles" 
            :key="file.name"
            @click="selectFile(file)"
            :class="['file-item', { active: selectedFile?.name === file.name }]"
          >
            <span class="file-icon">📄</span>
            <span class="file-name">{{ formatFileName(file.name) }}</span>
          </button>
        </div>
      </div>

      <!-- Content Display -->
      <div class="docs-content">
        <div v-if="selectedFile" class="content-header">
          <h2 class="content-title">{{ formatFileName(selectedFile.name) }}</h2>
          <div class="content-meta">
            <span class="file-size">{{ formatFileSize(selectedFile.size) }}</span>
            <span class="file-date">{{ formatDate(selectedFile.lastModified) }}</span>
          </div>
        </div>
        
        <div v-if="loading" class="loading-state">
          <div class="spinner"></div>
          <p>{{ t('loadingDocumentation') }}</p>
        </div>
        
        <div v-else-if="selectedFile" class="markdown-content" v-html="renderedContent"></div>
        
        <div v-else class="empty-state">
          <h3>{{ t('selectDocumentToView') }}</h3>
          <p>{{ t('chooseFileFromSidebar') }}</p>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import { useAboutTranslations } from '@/composables/useAboutTranslations'

export default {
  name: 'AboutNotes',
  setup() {
    const { t } = useAboutTranslations()
    return { t }
  },
  data() {
    return {
      docFiles: [],
      selectedFile: null,
      renderedContent: '',
      loading: false
    }
  },
  // ... rest of existing methods
}
</script>

<!-- ... existing styles -->
```

### 4. Greenfire.vue (Team Information Tab)

**File:** `src/components/modals/about/Greenfire.vue`

#### Current Code (Lines 6, 14, 21):
```html
<label class="font-semibold"> Greenfire Management: </label>
<label class="font-semibold"> Developers: </label>
<label class="font-semibold"> Special Thanks To: </label>
```

#### Updated Code:
```vue
<template>
  <div class="w-full min-h-0 container flex flex-col">
    <div class="about-section row px-20 flex-grow">
      <div class="col flex flex-col justify-center">
        <div class="mb-2">
          <label class="font-semibold">{{ t('greenfireManagement') }}: </label>
          <ul class="ml-4">
            <li>Corey S.</li>
            <li>Nicole L.</li>
            <li>Uzma B.</li>
          </ul>
        </div>
        <div class="mb-2">
          <label class="font-semibold">{{ t('developers') }}: </label>
          <ul class="ml-4">
            <li>Aciel O.</li>
            <li>Tim R.</li>
          </ul>
        </div>
        <div class="mb-2">
          <label class="font-semibold">{{ t('specialThanksTo') }}: </label>
          <ul class="ml-4">
            <li>{{ t('pastDevelopers') }}</li>
            <li>{{ t('pastGraphicDesigners') }}</li>
          </ul>
        </div>
      </div>
      <div class="col center-content">
        <img
          src="/assets/icons/greenfire/greenfire.svg"
          class="logo"
          :alt="t('greenfireLogo')"
        />
      </div>
    </div>
    <div class="footer flex-shrink-0">
      <a
        v-for="link of links"
        :key="link.name + '-link'"
        class="footer-link"
        :href="link.url"
        target="_blank"
        >{{ getTranslatedLinkName(link.name) }}</a
      >
    </div>
  </div>
</template>

<script>
import { useAboutTranslations } from '@/composables/useAboutTranslations'

const link = (name, url) => ({ name, url })

export default {
  name: 'AboutGreenfire',
  setup() {
    const { t } = useAboutTranslations()
    return { t }
  },
  data: () => ({
    links: [
      link('Website', 'https://www.greenfire.io'),
      link('Twitter', 'https://twitter.com/greenfire_io'),
      link('Instagram', 'https://www.instagram.com/greenfire.io'),
      link('LinkedIn', 'https://www.linkedin.com/company/greenfire-inc'),
      link('Keybase', 'https://keybase.io/team/greenfire'),
      link('Github', 'https://github.com/greenfireinc'),
      link('Medium', '#')
    ]
  }),
  methods: {
    getTranslatedLinkName(name) {
      const linkTranslations = {
        'Website': this.t('website'),
        'Twitter': this.t('twitter'),
        'Instagram': this.t('instagram'),
        'LinkedIn': this.t('linkedin'),
        'Keybase': this.t('keybase'),
        'Github': this.t('github'),
        'Medium': this.t('medium')
      }
      return linkTranslations[name] || name
    }
  }
}
</script>

<!-- ... existing styles -->
```

### 5. Universe.vue (Projects Showcase Tab)

**File:** `src/components/modals/about/Universe.vue`

#### Current Code (Lines 3, 13, 25, 37, 48-50):
```html
<h3 class="text-lg font-semibold text-center mb-4">Greenfire Universe</h3>
<p class="project-description">Bridging the cryptocurrency ecosystem with the real world</p>
<p class="project-description">A cryptocurrency bookkeeping and accounting wallet</p>
<p class="project-description">A marketplace to buy and sell electronics with crypto</p>
<h4 class="project-title">*Redacted*</h4>
<p class="project-description">Coming soon...</p>
```

#### Updated Code:
```vue
<template>
  <div class="universe-container">
    <h3 class="text-lg font-semibold text-center mb-4">{{ t('greenfireUniverse') }}</h3>
    
    <div class="projects-grid">
      <!-- Greenfire -->
      <div class="project-card" @click="openUrl('https://greenfire.io')">
        <div class="logo-container">
          <img src="/assets/icons/greenfire/greenfire.svg" :alt="t('greenfireLogo')" class="project-logo" />
        </div>
        <div class="project-info">
          <h4 class="project-title">Greenfire</h4>
          <p class="project-description">{{ t('bridgingCryptocurrencyEcosystem') }}</p>
          <div class="project-url">https://greenfire.io</div>
        </div>
      </div>
      
      <!-- Greenery -->
      <div class="project-card" @click="openUrl('https://greenery.finance')">
        <div class="logo-container">
          <img src="/assets/icons/greenfire/greenery.svg" :alt="t('greeneryLogo')" class="project-logo" />
        </div>
        <div class="project-info">
          <h4 class="project-title">Greenery</h4>
          <p class="project-description">{{ t('cryptocurrencyBookkeepingWallet') }}</p>
          <div class="project-url">https://greenery.finance</div>
        </div>
      </div>
      
      <!-- Techmarkets -->
      <div class="project-card" @click="openUrl('https://techmarkets.io')">
        <div class="logo-container">
          <img src="/assets/icons/greenfire/greenfire.svg" :alt="t('techmarketsLogo')" class="project-logo" />
        </div>
        <div class="project-info">
          <h4 class="project-title">Techmarkets</h4>
          <p class="project-description">{{ t('marketplaceElectronicsCrypto') }}</p>
          <div class="project-url">https://techmarkets.io</div>
        </div>
      </div>
      
      <!-- Redacted -->
      <div class="project-card redacted">
        <div class="logo-container">
          <div class="redacted-logo">?</div>
        </div>
        <div class="project-info">
          <h4 class="project-title">{{ t('redacted') }}</h4>
          <p class="project-description">{{ t('comingSoon') }}</p>
          <div class="project-url">{{ t('redacted') }}</div>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import { useAboutTranslations } from '@/composables/useAboutTranslations'

export default {
  name: 'UniverseView',
  setup() {
    const { t } = useAboutTranslations()
    return { t }
  },
  methods: {
    openUrl(url) {
      // Use electron shell to open external URL
      if (window.shell) {
        window.shell.openExternal(url);
      } else if (window.open) {
        window.open(url, '_blank');
      }
    }
  }
}
</script>

<!-- ... existing styles -->
```

### 6. Contribute.vue (Contribution Guide Tab)

**File:** `src/components/modals/about/Contribute.vue`

#### Current Code (Lines 3, 6-7, 12-13, 22, 40-41):
```html
<h2 class="text-2xl font-semibold mb-4">Contribute to Sparkplate</h2>
<h3 class="text-xl font-medium mb-2">GitHub Repository</h3>
<p class="mb-2">Sparkplate is an open source project. You can contribute by submitting pull requests or reporting issues on GitHub.</p>
<h3 class="text-xl font-medium mb-2">How to Clone the Repository</h3>
<p class="mb-2">To get started with development, clone the repository to your local machine:</p>
<h3 class="text-xl font-medium mb-2">How to Submit a Pull Request</h3>
<h3 class="text-xl font-medium mb-2">Reporting Issues</h3>
<p class="mb-2">Found a bug or have a feature request? Please report it on our issue tracker:</p>
```

#### Updated Code:
```vue
<template>
  <div class="contribute-container">
    <h2 class="text-2xl font-semibold mb-4">{{ t('contributeToSparkplate') }}</h2>
    
    <div class="mb-6">
      <h3 class="text-xl font-medium mb-2">{{ t('githubRepository') }}</h3>
      <p class="mb-2">{{ t('openSourceProject') }}</p>
      <p class="mb-2">{{ t('repository') }}: <code>git@github.com:GreenfireInc/Sparkplate.Fresh.git</code></p>
    </div>

    <div class="mb-6">
      <h3 class="text-xl font-medium mb-2">{{ t('howToCloneRepository') }}</h3>
      <p class="mb-2">{{ t('getStartedDevelopment') }}</p>
      <pre class="bg-gray-100 p-3 rounded-md text-sm overflow-x-auto">git clone git@github.com:GreenfireInc/Sparkplate.Fresh.git
cd Sparkplate.Fresh
nvm use 23
npm install
npm run dev</pre>
    </div>

    <div class="mb-6">
      <h3 class="text-xl font-medium mb-2">{{ t('howToSubmitPullRequest') }}</h3>
      <ol class="list-decimal pl-5 space-y-2">
        <li>{{ t('createNewBranchForFeature') }}:
          <pre class="bg-gray-100 p-2 rounded-md text-sm mt-1">git checkout -b yourInitials.Section.featureFunctionName</pre>
        </li>
        <li>{{ t('makeChangesCommitDescriptive') }}:
          <pre class="bg-gray-100 p-2 rounded-md text-sm mt-1">git commit -m "Add new feature: description of changes"</pre>
        </li>
        <li>{{ t('pushBranchToGitHub') }}:
          <pre class="bg-gray-100 p-2 rounded-md text-sm mt-1">git push origin yourInitials.Section.featureFunctionName</pre>
        </li>
        <li>{{ t('goToSparkplateRepository') }} <a href="https://github.com/GreenfireInc/Sparkplate.Fresh" class="text-blue-600 hover:underline">Sparkplate GitHub repository</a> {{ t('clickPullRequestButton') }}</li>
        <li>{{ t('fillOutPRTemplateDetails') }}</li>
        <li>{{ t('submitPullRequestWait') }}</li>
      </ol>
    </div>

    <div class="mb-6">
      <h3 class="text-xl font-medium mb-2">{{ t('reportingIssues') }}</h3>
      <p class="mb-2">{{ t('foundBugFeatureRequest') }}</p>
      <ol class="list-decimal pl-5 space-y-2">
        <li>{{ t('visitIssuesPageGitHub') }} <a href="https://github.com/GreenfireInc/Sparkplate.Fresh/issues" class="text-blue-600 hover:underline">Issues page</a> on GitHub.</li>
        <li>{{ t('clickNewIssueButton') }}</li>
        <li>{{ t('selectAppropriateTemplate') }}</li>
        <li>{{ t('fillOutAllRequired') }}:
          <ul class="list-disc pl-5 mt-1">
            <li>{{ t('clearDescriptionIssue') }}</li>
            <li>{{ t('stepsToReproduceBugs') }}</li>
            <li>{{ t('expectedBehavior') }}</li>
            <li>{{ t('actualBehavior') }}</li>
            <li>{{ t('environmentDetailsOS') }}</li>
            <li>{{ t('screenshotsErrors') }}</li>
          </ul>
        </li>
      </ol>
    </div>
  </div>
</template>

<script>
import { useAboutTranslations } from '@/composables/useAboutTranslations'

export default {
  name: 'ContributeView',
  setup() {
    const { t } = useAboutTranslations()
    return { t }
  }
}
</script>

<!-- ... existing styles -->
```

### 7. Donations.vue (Support Tab)

**File:** `src/components/modals/about/Donations.vue`

#### Current Code (Lines 4-7, 22, 44):
```html
<h2 class="text-xl font-bold">Support Sparkplate</h2>
<p class="text-sm text-muted">
  Your donations help us maintain and improve this free resource for everyone.
</p>
:alt="`QR code for ${getSymbolFromAddress(hoveredQR)}`" 
:alt="`${getSymbolFromAddress(wallet.address)} logo`" 
```

#### Updated Code:
```vue
<template>
  <div class="donations-container">
    <div class="text-center mb-6">
      <h2 class="text-xl font-bold">{{ t('supportSparkplate') }}</h2>
      <p class="text-sm text-muted">
        {{ t('donationsHelpMaintainFree') }}
      </p>
    </div>
    
    <!-- Hover overlay for centered QR codes -->
    <div 
      v-if="hoveredQR && shouldCenterMagnify(hoveredQR)"
      class="qr-overlay"
      @mouseenter="clearHoverTimeout"
      @mouseleave="scheduleHoverTimeout"
    >
      <div class="magnified-qr-container">
        <button @click="hoveredQR = null" class="close-btn">&times;</button>
        <img 
          :src="generateQRCode(getAddressWithoutPrefix(hoveredQR))" 
          :alt="`${t('qrCodeFor')} ${getSymbolFromAddress(hoveredQR)}`" 
          class="magnified-qr-code"
        />
        <div class="magnified-logo-overlay">
          <div class="magnified-logo-container">
            <img 
              :src="getLogoPath(getSymbolFromAddress(hoveredQR))" 
              :alt="`${getSymbolFromAddress(hoveredQR)} ${t('logo')}`" 
              class="magnified-crypto-logo"
            />
          </div>
        </div>
      </div>
    </div>
    
    <div class="crypto-wallets-grid">
      <div 
        v-for="wallet in cryptoWallets" 
        :key="wallet.address" 
        class="wallet-card"
        :class="{ 'opacity-30': hoveredQR && hoveredQR !== wallet.address }"
      >
        <span class="font-bold text-sm">{{ getSymbolFromAddress(wallet.address) }}</span>
        
        <div 
          class="qr-container"
          :class="{ 
            'opacity-30': hoveredQR && hoveredQR !== wallet.address,
            'hidden-when-centered': hoveredQR === wallet.address && shouldCenterMagnify(wallet.address)
          }"
          @mouseenter="handleQRHover(wallet.address)"
          @mouseleave="scheduleHoverTimeout"
        >
          <img 
            :src="generateQRCode(getAddressWithoutPrefix(wallet.address))" 
            :alt="`${t('qrCodeFor')} ${getSymbolFromAddress(wallet.address)}`" 
            class="qr-code"
          />
          
          <!-- Logo overlay -->
          <div class="logo-overlay">
            <div class="logo-container">
              <img 
                :src="getLogoPath(getSymbolFromAddress(wallet.address))" 
                :alt="`${getSymbolFromAddress(wallet.address)} ${t('logo')}`" 
                class="crypto-logo"
              />
            </div>
          </div>
        </div>
        
        <div class="address-section">
          <div class="address-text">{{ getAddressWithoutPrefix(wallet.address) }}</div>
          <button 
            @click="copyAddressToClipboard(wallet.address)" 
            class="copy-btn"
            :class="{ 'copied': copiedAddress === wallet.address }"
          >
            {{ copiedAddress === wallet.address ? t('copied') : t('copyAddress') }}
          </button>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import { useAboutTranslations } from '@/composables/useAboutTranslations'

export default {
  name: 'DonationsView',
  setup() {
    const { t } = useAboutTranslations()
    return { t }
  },
  // ... rest of existing data and methods
}
</script>

<!-- ... existing styles -->
```

## 🔄 Language Selector Integration

### Add Language Selector to About Modal

You can add a language selector to the About modal header by modifying `About.vue`:

```vue
<template>
  <div class="modal-header">
    <h4 class="h4 font-semibold mb-0">{{ t('sparkplate') }} {{ version }}</h4>
    
    <!-- Language Selector -->
    <div class="language-selector">
      <select 
        v-model="currentLanguage" 
        @change="changeLanguage(currentLanguage)"
        class="language-select"
      >
        <option v-for="lang in availableLanguages" :key="lang.code" :value="lang.code">
          {{ lang.flag }} {{ lang.name }}
        </option>
      </select>
    </div>
  </div>
</template>

<script>
import { useAboutTranslations, useAboutLanguageSelector } from '@/composables/useAboutTranslations'

export default {
  // ... existing code
  setup() {
    const { t } = useAboutTranslations()
    const { 
      currentLanguage, 
      availableLanguages, 
      changeLanguage 
    } = useAboutLanguageSelector()
    
    return {
      t,
      currentLanguage,
      availableLanguages,
      changeLanguage
    }
  }
  // ... rest of component
}
</script>

<style lang="scss" scoped>
.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-bottom: 1rem;
  border-bottom: 1px solid #e5e5e5;
}

.language-selector {
  .language-select {
    @apply px-3 py-1 border border-gray-300 rounded text-sm;
    min-width: 120px;
  }
}
</style>
```

## ✅ Testing Checklist

After implementing the translations:

- [ ] All text displays in the selected language
- [ ] Language switching works in real-time
- [ ] Language preferences persist across sessions
- [ ] No missing translations (no raw keys displayed)
- [ ] UI layouts accommodate different text lengths
- [ ] Special characters display correctly
- [ ] All 5 languages work properly

## 🎯 Next Steps

1. **Implement** the changes in each component
2. **Test** all languages thoroughly
3. **Add** language selector to About modal header
4. **Verify** language persistence works
5. **Check** UI layouts with longer translations
6. **Document** any additional customizations needed

---

**Integration Status:** Ready for Implementation  
**Estimated Time:** 2-3 hours for full integration  
**Testing Time:** 1 hour for comprehensive testing
